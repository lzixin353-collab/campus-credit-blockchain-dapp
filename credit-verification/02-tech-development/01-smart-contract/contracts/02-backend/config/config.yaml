# 服务器配置
server:
  port: 8080
  mode: debug  # 开发环境用debug，生产用release

# MySQL配置
mysql:
  dsn: "root:your_mysql_password@tcp(127.0.0.1:3306)/credit_dapp?charset=utf8mb4&parseTime=True&loc=Local"
  max_open_conns: 100
  max_idle_conns: 20
  conn_max_lifetime: 3600

# 以太坊/合约配置
ethereum:
  rpc_url: "http://127.0.0.1:8545"  # 本地节点RPC，固定值
  credit_contract_addr: "0xCf7Ed3AccA5a467e9e704C703E8D87F634fB0Fc9"  # 替换为你的CreditContract地址
  role_contract_addr: "0x9fE46736679d2D9a65F0992F2272dE9f3c7fa6e0"  # 替换为你的RoleContract地址
  private_key: "your_private_key" # 替换为本地节点第一个钱包私钥

# JWT配置
jwt:
  secret: "credit-dapp-secret-2026"  # 自定义密钥
  expire_hours: 24